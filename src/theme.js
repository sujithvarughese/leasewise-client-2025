import { createTheme } from '@mantine/core'

const theme = createTheme({
  fontFamily: 'Trebuchet, Verdana, sans-serif',
  fontFamilyMonospace: 'Trebuchet, Monaco, Courier, monospace',
  headings: { fontFamily: 'Trebuchet, Greycliff CF, sans-serif' },
})

export default theme